-- KSR Auto's — Aggregate schema
-- For first-time setup run the migration files in order:
--   supabase/migrations/0001_init.sql
--   supabase/migrations/0002_seed.sql   (optional)
--
-- This file is kept as a single-import convenience and only re-exports the
-- migrations. Use the migration files when running via the Supabase CLI.

\i 'migrations/0001_init.sql'
-- Uncomment to seed example data:
-- \i 'migrations/0002_seed.sql'
